import React from 'react'
const DataContext= React.createContext()
export default DataContext

// have to have this in for useContext 
// no longer can use export default data function 
// this is going to create PROVIDER and CONSUMER

